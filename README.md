# App That let you Add,Remove,Edit Employees in your DB File!
# The app Contains Flags like
# -f = (req!) for new file name
# -n = is that a new file or not (true or false)
# -a = add employee information